function $(selected) {
  return document.getElementById(selected);
}
function $$(selected) {
  return document.querySelectorAll(selected);
}
function $$$(selected) {
  return document.querySelector(selected);
}
function creaElm(dom) {
  return document.createElement(dom);
}
